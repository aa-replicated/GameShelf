apiVersion: kots.io/v1beta2
kind: HelmChart
metadata:
  name: gameshelf
spec:
  chart:
    name: gameshelf
    chartVersion: ""
  values:
    image:
      tag: repl{{ '{{' }} .VersionLabel {{ '}}' }}
    global:
      imagePullSecrets:
        - name: '{{repl ImagePullSecretName }}'
