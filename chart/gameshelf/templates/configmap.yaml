apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gameshelf.fullname" . }}
  labels:
    {{- include "gameshelf.labels" . | nindent 4 }}
data:
  port: "8080"
  site-name: {{ .Values.siteName | quote }}
